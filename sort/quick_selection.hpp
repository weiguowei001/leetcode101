#pragma once
#include <vector>

using namespace std;

namespace algorithm {
namespace sort {

int quickSelection(vector<int> &nums, int l, int r);
int findKthLargest(vector<int> &nums, int k);

} // namespace sort
} // namespace algorithm
