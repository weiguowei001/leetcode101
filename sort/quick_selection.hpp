#pragma once
#include <vector>

namespace algorithm {
namespace sort {

int quickSelection(std::vector<int> &nums, int l, int r);
int findKthLargest(std::vector<int> &nums, int k);

} // namespace sort
} // namespace algorithm
